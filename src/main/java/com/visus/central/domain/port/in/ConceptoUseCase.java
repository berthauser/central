package com.visus.central.domain.port.in;

import com.visus.central.domain.model.Concepto;

public interface ConceptoUseCase extends CrudUseCase<Concepto>{
	// Los métodos findAll(), findById(), save(), deleteById() 
    // ya están definidos en CrudUseCase, así que puedes eliminarlos
    // o dejarlos como documentación (no es necesario)
}
